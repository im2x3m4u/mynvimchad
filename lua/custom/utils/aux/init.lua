local aux = require "custom.utils.aux.aux"

aux.packer = require "custom.utils.aux.packer"

aux.lspconfig = require "custom.utils.aux.lspconfig"

aux.nvim_cmp = require "custom.utils.aux.nvim-cmp"

-- aux.actions = require("utils.aux.actions")

return aux
